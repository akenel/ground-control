# Worked examples — real sessions, unedited

*Raw recordings of actual Ground Control sessions. Not curated docs — the real
thing, keystroke by keystroke, so you can watch the method work (and stumble, and
recover) on a real build.*

- **[session-01-tempest-in-a-day.md](session-01-tempest-in-a-day.md)** — the session
  that built **TIG · Tempest** from an empty folder to a playable, published arcade
  game in one sitting: filling the context spine, seven human-green'd build steps,
  then live on GitHub Pages. ~2,700 lines. This is the first of its kind — start here.

Want the short, curated version of a single loop first? See the kit's
[`worked-example.md`](../../kit/worked-example.md), then come back and watch a whole
build unfold for real.

> The game is the trailer. This folder is the making-of. The rest of the repo is the
> method that made both. **Play it, then read how it was built.**
